# Firmware

**To be added.**

Planned: C/C++ firmware for the ESP32-S3 using ESP-IDF and ESP-DSP (48 kHz sampling, 8192-point FFT, phase-locked coherent averaging, adaptive frame rejection, per-ear in-situ calibration, BLE, OLED UI).

When added, please document the ESP-IDF version, build/flash steps and hardware revision this firmware targets.
